/*
** EPITECH PROJECT, 2024
** mypaint
** File description:
** maths.c
*/

#include "../includes/my.h"

int max(int a, int b)
{
    return ((a > b) ? a : b);
}

int min(int a, int b)
{
    return ((a < b) ? a : b);
}
